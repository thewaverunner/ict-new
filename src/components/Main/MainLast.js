import React from 'react';
import Button from '../Ui/Button';

const MainLast = () => (
  <section className="main__last container">
    <p>
      The network model bussines
      <br />
      model has been around of decades.
      <br />
      And it works.
    </p>
    <Button type="black">Learn more</Button>
  </section>
);


export default MainLast;
